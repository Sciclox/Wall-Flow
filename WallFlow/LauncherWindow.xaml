<Window x:Class="WallFlow.LauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True"
        ShowInTaskbar="False" Topmost="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Width="520"
        ResizeMode="NoResize"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        PreviewKeyDown="Window_PreviewKeyDown"
        Deactivated="Window_Deactivated">
    <Border CornerRadius="14" Background="#2D2D2D" BorderBrush="#404040" BorderThickness="1" Padding="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.6" BlurRadius="24" ShadowDepth="8" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" CornerRadius="10" Background="#1E1E1E" BorderBrush="#404040" BorderThickness="1" Padding="4" Margin="0,0,0,8">
                <Grid>
                    <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                               Foreground="#666666" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0,0,0" />
                    <TextBox x:Name="SearchBox" FontFamily="Segoe UI" FontSize="18" Foreground="White"
                             Background="Transparent" BorderThickness="0" Padding="32,6,6,6"
                             CaretBrush="White" IsTabStop="False"
                             TextChanged="SearchBox_TextChanged" />
                </Grid>
            </Border>
            <ListBox x:Name="ResultsList" Grid.Row="1"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MaxHeight="350"
                     ItemTemplate="{StaticResource WallpaperItemTemplate}"
                     PreviewMouseLeftButtonUp="ResultsList_PreviewMouseLeftButtonUp" />
        </Grid>
    </Border>
</Window>
